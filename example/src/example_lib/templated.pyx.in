{{for typ in supported}}

# {{typ}} C adder ({{typ}})
cpdef {{typ}} c{{typ[:1]}}add({{typ}} a, {{typ}} b):
    return a + b

# {{typ}} C mul ({{typ}})
cpdef {{typ}} c{{typ[:1]}}mul({{typ}} a, {{typ}} b):
    return a * b

# {{typ}} C pow ({{typ}})
cpdef {{typ}} c{{typ[:1]}}pow({{typ}} a, {{typ}} b):
    return a ** b

{{endfor}}
